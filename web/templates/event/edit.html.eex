<h2>Edit event</h2>

<%= render "form.html", changeset: @changeset,
                        action: admin_event_path(@conn, :update, @event) %>

<%= link "Back", to: admin_event_path(@conn, :index) %>
